# CLAUDE.md

Nodi — 교실 학습용 AI 도우미. 학생이 질문하면 EXAONE이 **개념 카드**를 스트리밍하고,
무한 캔버스 위에 임베딩 기반으로 배치한다. 선생님 수업 자료·교과서를 근거로 답한다.

## 작업 실행 체계 (docs/) — Manager 전용

<subagent-guard>
**역할 가드**: 이 섹션은 사용자와 직접 대화하는 **메인 세션(Manager)에만**
적용된다. 당신이 Agent 도구로 디스패치된 **서브에이전트라면**(프롬프트에 task
브리프와 보고 지시가 있다) 이 섹션을 무시하라 — TASKS.md를 실행하지 말고,
에이전트를 생성하지 말고, PROCESS.md 루프를 돌리지 말고, **받은 브리프의 task
하나만 수행해 보고하라.** docs/AGENTS.md는 브리프가 명시적으로 가리키는 자기
페르소나 섹션만 참고한다.
</subagent-guard>

Manager는 기능 구현 작업 시 다음 문서 체계를 따른다 — **작업 시작 전에 반드시 읽는다**:

- `docs/TASKS.md` — **무엇을**: 큰 작업 단위 목록 (위에서부터 하나씩 수행)
- `docs/AGENTS.md` — **누가**: 페르소나 × 스킬 (Manager 전담 / 병렬 에이전트)
- `docs/PROCESS.md` — **어떻게**: 실행 루프 (분해 → 병렬 디스패치 → 리뷰 게이트 → 기록)

## 제품 모델 (워크스페이스 중심)

- **선생님**은 워크스페이스(= `space_kind='class'` 학급)를 개설하고, 수업에 사용할
  **교과서·학습 자료**를 업로드한다. 이 파일(`kind='class_material'`)은
  청킹 → 임베딩(Qdrant)으로 **RAG로 구축**되어, 질의 시 top-K 청크가 근거로 주입된다.
- 선생님은 별도 버튼으로 **교과서**(`kind='textbook'`, PDF 전용)도 업로드한다
  (TASK 4, D86~D88 — 0038·0039 원격 적용 완료, E2E PASS 2026-07-17). 텍스트는
  class_material과 동일하게 RAG 구축 + 추가로 figure를 추출·임베딩해 Qdrant
  `textbook_figures`에 적재(D86). **figure 캡션은 비전 생성이 단독 확정한다**
  (D134, 사용자 결정 2026-07-30 — D93 후보 선택·D103 2단 경로·D131 노브 대체):
  비전 모델(judge_* 노브, 기본 EXAONE-4.5)이 페이지 본문
  (`textbook_figures.page_text`, split 시점 영속)을 컨텍스트로 캡션을 생성
  (`match_kind='generated'`)하고, 그 **생성 캡션 단독**이 임베딩 텍스트다.
  파서 라벨 캡션(정규화 거리 0.25 이내 `caption`/`footnote`)은 생성 프롬프트의
  힌트로만 쓰인다 — 폴백 아님. **생성 실패는 폴백 없이 failed(caption-error),
  추측하지 않는다.** 비전 미설정은 업로드를 막지 않지만(텍스트 RAG 정상)
  figure 전부가 no-caption failed(검색 미노출, env 설정 후 retry로 재생성 가능).
  학생 질의와 유사한
  figure(거리 게이트 0.60)는 캔버스 FigureNode로 표시 — **다중 표시**(D95:
  top-3, 리프 id=`figure-{figureId}`로 세션 내 중복 제거·누적, 재수화는 전
  노드 figures를 figureId dedupe 후 전부 복원). 이미지는 백엔드 signed
  URL로만 서빙, **URL 영속 금지**(재수화·만료 시 `GET /files/figures/{id}` 재발급,
  D87). figure 실패는 텍스트 인덱싱과 격리(`files.status` 불가침, D88).
- **학생**은 워크스페이스에 참여해(학급 코드 가입) 세션을 열고, 선생님이 올린
  교과서·자료를 근거로 질의한다. RLS가 학급 자료 접근을 통제한다.
- **학생도 워크스페이스 세션에 파일을 업로드할 수 있다** (TASK 3, D83~D85 구현
  완료). 이 파일(`kind='user_upload'`)은 RAG로 구축하지 않고 청킹(오버랩 0)·저장
  까지만 한 뒤(임베딩·Qdrant 생략) **해당 세션의 컨텍스트로 전문(全文) 주입**한다.
  예산은 세션당 합산 문자 튜너블 `session_context_max_chars`(기본 150K자, D84) —
  파싱 후 초과 파일은 한국어 사유와 함께 거부. 바이트 1차 상한(D77: 학생 50MB) 유지.
  마이그레이션 0037(files.session_id·context_chars, 'stored' 청크 상태)은
  **2026-07-16 원격 적용 완료**(사용자 승인).
- 학생은 워크스페이스 세션 외에 **개인 세션**(`space_kind='personal'`)을 개설해
  자유롭게 질의할 수 있다.
- 교과서 전역 코퍼스(admin 인제스트 경로)는 **2026-07-14 완전 제거됨**(TASK 1,
  스펙 `docs/superpowers/specs/2026-07-14-remove-textbook-rag-design.md`). admin은
  교과서를 입력하지 않는다 — 교과서는 선생님이 워크스페이스에 업로드한다(TASK 2).

## 스택

Next.js(App Router, `frontend/`) · FastAPI(`backend/`) · Postgres(RLS로 권한 강제·자체 인증)
· Qdrant(벡터 1024d/Cosine, `docker compose up -d qdrant`) · Upstage(임베딩 + 문서 파싱)
· Upstage `solar-pro3`(대화 생성 — 스트리밍 + tool calling, D108. 인제스트 시점
LLM 작업 — 원자 질문 생성 D129·의미 청킹 경계 판단 D132 — 도 이 모델을 재사용한다.
2026-08-04 사용자 지시로 solar-pro2에서 올렸다 — `UPSTAGE_CHAT_MODEL`로 되돌린다).
교과서 도판 비전만 별도 계열(judge_* 노브)이다 — OpenAI 호환 비전 엔드포인트면
무엇이든 꽂히고, 배포 서버는 자체 GPU에 llama.cpp로 EXAONE-4.5-33B를 띄워
쓴다(D118). 이 계열이 지금 하는 일은 **캡션 생성**이다(`figure_caption.py`,
D131·D134 — `figure_judge.py`는 설정 게이트·공용 유틸만 남았다).
base_url/model/api_key 셋이 다 채워져야 동작하고, 로컬은 비워 두면 된다(파서
라벨 경로만 동작).

## 핵심 파이프라인

- **파일 인제스트** (`services/worker/`, D105로 책임별 분할 —
  jobs·split·batch·figures·common·runner): 업로드(형식 화이트리스트 D75,
  용량 kind별 D77 — 교사 자료 500MB/학생 50MB) → `embedding_split` 잡
  → Upstage Document Parse(50MB 초과 PDF는 페이지 분할 파싱, D78)
  → 문단 인지 청킹(1,200자/오버랩 150자, admin 튜너블)
  → `embedding_batch` 잡 팬아웃(64청크 단위) → Upstage `embedding-passage` 1024d(D106)
  → **벡터는 Qdrant, 청크 본문·상태는 Postgres `file_chunks`**.
  **D132 의미 청킹**(`semantic_chunking_enabled`, 기본 off): on이면 소형 문서
  (`semantic_chunking_max_chars` 이하)에 한해 solar가 청크 경계를 재조정
  (`semantic_chunker.py`, PIKE resplit 이식) — 어떤 실패든 정규식 청킹 폴백.
  **D129 지식 원자화**(`atom_rag_enabled`, 기본 off): on이면 `atom_batch` 잡이
  청크당 solar로 예상 질문을 생성(`atomize.py`·`worker/atoms.py`)해
  `embedding-passage`로 Qdrant `chunk_atoms`에 적재(페이로드 `{atom_id, chunk_id,
  file_id, owner_id}`만, 행은 Postgres `chunk_atoms`). 원자 실패는 텍스트 인덱싱과
  격리(D88 동형). 장기 잡(원자 생성·캡션 생성·의미 청킹)은 `common.touch_job`
  하트비트로 스테일 복구(120초) 오탐을 막는다(D133).
  교과서는 `upstage.parse_document_full`(표준 모드+coordinates+figure base64 —
  D92로 enhanced 제거, 조각 ≤48MB·≤100p 사전 분할)로 텍스트·elements를 한 번에
  얻고 figure 팬아웃(`figure_batch` 잡, 배치 8): 크롭 Storage 업로드 → 비전 캡션
  생성(D134 단독 경로 — 이미지 + `page_text` + 파서 라벨 힌트로 생성,
  `match_kind='generated'`, 실패는 폴백 없이 failed) → `embedding-passage` →
  Qdrant `textbook_figures`(**페이로드는 `{figure_id, file_id, owner_id}`만**).
  행 상태는 `textbook_figures.status`로만 추적(D86/D88).
- **채팅 턴** (`routers/chat.py` `chat_stream`) — 경로가 둘이다:

  **ReAct 경로** (D109, `react_enabled` 튜너블·**기본 on**): 도구 판단 → 스킬 실행
  → 생성. 스킬은 `app/ai/skills/`에 파일 하나씩이고, 노출 카탈로그는
  `ai/catalog.py`가 `(space_kind, role)`로 **먼저 좁힌다** — 개인 세션에 학급
  도구를 보여주면 모델이 부르고 빈 결과로 엉뚱한 답을 한다. 좁힌 뒤 모델이
  고르므로 인사 턴에는 임베딩·검색이 아예 나가지 않는다. 판단 단계는 개념 카드
  형식을 주지 않고, 생성 단계는 도구를 주지 않는다(두 지시의 충돌 회피 +
  추론 누출 차단). 스킬 실패는 `SkillResult(ok=False)`로 모델에 전달되고
  턴을 죽이지 않는다.

  스킬 8종: think · search_class_material · search_textbook_figure ·
  search_lecture_clip · list_session_concepts · get_concept ·
  list_session_files · read_session_file.
  카탈로그는 스코프뿐 아니라 **세션 상태**로도 갈린다(파일이 없으면 파일 스킬을
  노출하지 않는다 — 노출하면 모델이 부르고 빈 결과로 군더더기를 붙인다).
  판단 프롬프트에 복합 질문 서브질문 분해 지침이 있다(D130). search_class_material은
  노브 둘을 더 탄다(둘 다 기본 off): `rag_query_rewrite_enabled` — solar 1콜로
  검색어 정제(실패 시 원문), `atom_rag_enabled` — `rag.dual_search`로 청크·원자
  이중 검색(거리 게이트 분리: 직접 0.60 / 원자 `atom_rag_max_distance` 0.45,
  원자 경유 청크 재게이트 금지 — 게이트는 dual_search 내부에서 끝난다, D129).

  **곁들이 둘은 모델 선택에 맡기지 않는다 (D163).** 교과서 도판·강의 클립은
  답을 바꾸지 않고 화면에 **곁들여 뜨는 것**인데, 모델은 이 도구를 자주 건너뛴다
  (실측 2026-08-03: "정확도랑 정밀도 차이가 뭐야?"에 도구 0건 — 거리 0.49로
  걸리는 클립이 있는데도). 그래서 **개념 카드가 나온 턴이면** 안 부른 쪽을
  오케스트레이터가 대신 부른다. 판정 신호가 "생성 결과"인 이유는 인사 턴에
  임베딩이 나가면 안 되기 때문이다 — 인사에는 개념 카드가 없다. 모델이 자기가
  다듬은 검색어로 **빈손이면 원문으로 한 번 더** 찾는다(D135 동형 문제).

  **기존 단발 경로** (`react_enabled` off, 롤백용):
  컨텍스트 빌더 병렬(gather): 기억 연결·파일 RAG·비교 참조·세션 파일 전문
  (D83, `session_context.py` — session_files 블록은 system_base 직후 고정,
  D85 Friendli 프리픽스 캐시) →
  `gemini.compose_system_structured`(D35: 프롬프트 문자열 + 블록 span 단일 소스.
  세션 기사용 분류 태그를 tag_guide 블록으로 주입해 태그 재사용 유도 — D89,
  session_files 뒤 배치로 캐시 프리픽스 보존) →
  `exaone.stream_answer` SSE → 개념 카드 파싱·캔버스 배치. 시스템 프롬프트는
  **중·고등 전 교과 교사 페르소나 + 자유 분류 태그**(D89, `exaone.py`).
- **캔버스 v2 (D120~D128)**: 카드를 없애고 **학생이 편집하는 작업 공간**으로
  바꿨다. 평소에는 글자만 있고 hover 시 반투명 박스가 깔린다. 좌측 세로 괘선의
  색이 출처를 나타낸다 — **오커=AI, 틸=학생**(장식이 아니라 정보다. 색만으로는
  축소했을 때 약해서 굵기라는 두 번째 신호를 겹쳐 둔다).
  · **글은 손으로 써진다**(D164, 사용자 지시 2026-08-03). 폰트는 **나눔손글씨
    야근하는 김주임**(`.canvas2 .hand`, 2026-08-04 사용자 지시로 Gaegu 대체).
    **캔버스 위의 글에만** 쓴다(제목 + 본문 + 편집기). D127이 손글씨를 걷어낸
    근거는 그대로라 UI 크롬(`--font-ui`)·라벨(`--font-label`)·앱의 나머지
    화면에는 번지지 않는다 — E2E가 `body`에 이 폰트가 없음을 확인한다.
    · **자체 호스팅이라 쪼개는 것이 우리 일이다.** Google Fonts에 없어
      `scripts/build-hand-font.py`가 woff2 세 조각(라틴 27KB · KS X 1001 한글
      497KB · 나머지 음절 2.3MB)과 `components/canvas2/hand-font.css`를 만든다.
      교실 한국어는 KS 안에서 끝나 보통 524KB만 받는다(단일 파일은 2.9MB).
      그 CSS는 **CanvasStage가 임포트한다** — globals.css에 넣으면 unicode-range
      목록(gzip 13KB)이 로그인·홈·관리자 화면까지 따라간다.
    · **크기 보정을 걷어냈다.** Gaegu용 `font-size: 1.12em`을 그대로 두면 글이
      20% 커진다 — 실측(upm 1000)으로 김주임이 Gaegu보다 한글 글자가 17% 크고
      16% 좁다. 1em에서 실효 높이가 종전과 같아지고, 좁아진 만큼 자간만 벌린다.
    · **굵게는 합성 볼드다.** Gaegu를 골랐던 이유가 "한글 손글씨 중 굵기가 둘
      이상인 거의 유일한 선택"이었는데 김주임은 Regular 하나뿐이다. 본문
      `**굵게**`가 뭉개져 보이면 굵기 대신 다른 신호로 바꿔야 한다.
    · 애니메이션은 **글자 단위 wipe**다(`lib/canvas2/ink.ts`). 획 순서로 그리려면
      글리프마다 SVG 경로가 있어야 하고 한글 11,172자에는 불가능하므로, 글자
      하나가 왼쪽→오른쪽으로 드러나는 것으로 근사한다. `useTypewriter`가
      **몇 글자를 드러낼지**를, `ink.ts`가 **그중 어디를 쪼갤지**를 정한다.
    · **꼬리만 쪼갠다.** `toRuns`는 DOM 폭발을 막으려 같은 서식을 한 런으로
      합치는데 애니메이션은 글자마다 필요하다 — 방금 드러난 마지막 INK_TAIL(96)
      자만 개별 span으로 만들고 나머지는 다시 런으로 둔다.
    · **span의 key는 문서 전체 기준 절대 색인이다.** 배열 위치로 key를 매기면
      꼬리가 나아갈 때마다 span이 다른 글자로 재사용돼 **이미 다 써진 글이 매
      프레임 다시 써진다**(화면이 깜빡이는 것으로만 보여 놓치기 쉽다).
      `ink.test.ts`가 "덧붙여도 앞 글자의 색인은 안 변한다"를 지킨다.
    · **세 상수가 묶여 있다** — `MAX_PER_FRAME`(6) × 60fps × 애니메이션
      0.22s ≈ 80 < `INK_TAIL`(96). 어느 하나를 올리면 아직 써지는 중인 글자가
      꼬리 창 밖으로 밀려나 완성 상태로 툭 튄다.
    · 재수화는 **절대 다시 쓰지 않는다**(`tailFrom = Infinity`). 새로고침마다
      온 캔버스가 다시 써지면 학생이 이미 읽은 글을 또 기다린다.
    · **쓰는 자리에는 펜이 있다**(D165, studdyai.com 참고). 막대 캐럿은 기울여
      놓아도 "커서"로 읽힌다. 펜 그림이 하는 일은 장식이 아니라 **글자 wipe의
      세로 경계를 가리는 것**이다 — 경계가 보이면 잘린 글자로, 펜이 있으면
      나오는 중으로 읽힌다. 앵커는 **폭 0** 인라인 상자(글 끝 위치를 공짜로
      얻는다)이고 펜은 절대 배치로 흐름 밖에 뜬다 — 인라인에 넣으면 줄바꿈이
      흔들리고, 아이템에 overflow를 걸면 잘린다. `transform-origin`은 **펜촉**
      좌표(9% 91%)여야 한다. 몸통 가운데면 흔들 때 촉이 글자에서 떨어진다.
    · **손글씨가 못 그리는 글자만 크기를 맞춘다**(D165, `lib/canvas2/handScript.ts`).
      김주임은 한글·아스키만 덮어 한자·가나가 폴백으로 떨어지며 한글보다
      **1.16~1.19배 크게** 그려진다(실측 100px). 표의문자는 em 상자를 채우는
      단일 지표라 배율 하나(0.86)가 그대로 맞는다. 그리스·키릴·수학기호는
      **일부러 둔다** — 폴백 폰트는 대문자를 1.18배 키우고 소문자를 0.79배
      줄여야 해서 배율 하나로 못 맞추고, 작아 보이는 건 커 보이는 것보다 덜
      튄다. Studdy의 `-2px`를 베끼면 **틀린다**: 저쪽은 라틴·한글이 서로 다른
      두 폰트라 생긴 차이고, 우리는 한 폰트가 둘 다 덮는다.
  · **뷰포트는 Excalidraw가 소유한다**(D120). 팬/줌/그리기를 맡기고 우리 DOM
    오버레이를 얹는다: `screen = (world + scroll) * zoom`. 오버레이 컨테이너는
    `pointer-events:none`, 아이템만 `auto`이며 그리기 도구일 때는 아이템도
    놓아 준다(글 위에도 선을 그을 수 있어야 한다). 오버레이는 `z-index:3`이
    **반드시** 필요하다 — Excalidraw 캔버스가 2라서, 없으면 글이 보이는데
    눌리지 않는다(합성 이벤트는 히트 테스트를 건너뛰어 자동 검증에 안 잡힌다).
  · **팬 중에는 React를 돌리지 않는다**(D124). 카메라 rAF 폴링이 매 프레임
    setState하면 지도·연결선·아이템이 통째로 다시 돈다("화면 이동이 답답하다"의
    정체였다). 오버레이 변환과 격자는 `bridge.subscribeFrame`으로 DOM을 직접
    고치고, React state는 **멈춘 뒤 한 번**만 올린다(줌은 즉시).
  · **배치는 태그 열**(D123, `lib/canvas2/layout.ts`). 태그마다 열 하나(첫 등장
    순서로 고정), 열 안에서는 seq 순으로 위→아래. **무겹침이 알고리즘의
    성질이지 수렴의 결과가 아니다** — d3-force는 보장하지 못해 버렸다.
    폭도 실측값이다(`ITEM_W`는 상한). 아이템 폭은 `max-content`로 잡는다 —
    `fit-content`는 절대 배치에서 "컨테이너 폭 − left"를 가용 폭으로 삼아
    오른쪽 열의 글이 세로 기둥으로 찌그러진다.
  · **좌표를 저장한다**(D122, D105 폐기). `pinned=false`면 엔진이 자리를 정하고
    `pinned=true`(학생이 드래그한 것)면 장애물로만 읽는다.
  · **올가미 판정은 교차**(`rect.intersects`, 사용자 지시 2026-07-31 — 일부만
    걸쳐도 선택). 글과 도형이 **같은 규칙**을 쓴다. Excalidraw의 올가미는 완전
    포함이고 그 판정은 바꿀 수 없어서, 손을 뗄 때 우리가 계산한 결과를
    `selectedElementIds`로 덮어쓴다(`bridge.selectElementsIn`). 반드시 **다음
    프레임**에 써야 한다 — 우리 pointerup은 window 캡처라 저쪽보다 먼저 돈다.
    끄는 동안의 미리보기 강조만 저쪽 규칙으로 남는다.
  · **태그 하나 = 대화 트리 하나**(D151, `lib/canvas2/tree.ts` ·
    스펙 `docs/superpowers/specs/2026-08-02-tag-conversation-trees.md`).
    옛 Nodi의 노드 트리를 옮겼는데 단위가 다르다 — 저쪽은 세션 하나가 트리
    하나고 분기가 **컨텍스트를 줄이는** 장치였다. 여기서는 컨텍스트를 다
    넣고(사용자 결정 2026-08-02) 트리는 **순서와 초점**을 준다.
    간선은 `parent_item_id` 하나로 저장되고, **AI 개념 카드끼리 · 같은
    태그일 때만** 성립한다 — 부모가 최대 하나라 간선 수 = 노드 수 − 트리
    수다(거미줄이 될 수 없는 것이 알고리즘의 성질이다). 한 턴의 같은 태그
    카드는 형제가 아니라 **사슬**로 잇는다(생성 순서).
    · **초점(picked)은 선택과 다르다.** 선택은 옮기고 지우기 위한 것이고
      초점은 "이 가지에서 이어 묻는다"는 뜻이다. **끌면 고른 것이 아니다**
      (사용자 지시) — 움직이지 않고 누른 경우에만 잡힌다.
    · 배치는 열 **안**에서 트리 순서(깊이 우선), 들여쓰기는 **갈라질 때만**.
      사슬마다 들여쓰면 열 피치(700px)를 넘는다.
    · 지도는 축척으로 갈린다 — 축소는 태그 점, 확대(≥1.8배)는 노드 + 방향
      간선. "캔버스에도 선 보이게 하기" 토글은 **기본 켬**이고, 지도와
      ConnectorLayer가 같은 저장 키를 본다.
    · 프롬프트에 `tree_guide` 블록으로 **모든 카드를 태그별 트리 순서**로
      넣고, `focus_tag`로 지금 보는 트리만 가리킨다(자르지 않는다).
      **간선 판정이 프론트·백엔드 두 곳에 있다** — 한쪽만 고치면 화면의
      트리와 AI가 받는 순서가 갈리는데 화면에도 로그에도 안 드러난다.
      양쪽에 같은 케이스의 테스트가 있다.
  · 질문 원문은 **언제나** `data.askedQuestion`으로 답에 실어 hover 툴팁이
    쓴다 — 안 실으면 이어 물은 내용이 어디에도 남지 않는다(부모가 질문이
    아니라 앞 답이므로).
  · **곁들이는 카드에 딸린다**(D163). 강의 클립·교과서 도판은 그 턴 개념
    카드의 `parentItemId`를 받아 **오른쪽 옆**에 붙는다(`ATTACH_KINDS`).
    예전엔 부모가 없어 태그 없는 열(UNTAGGED)로 갔는데, 그 열은 태그 열들
    뒤라 카드에서 COL_GAP(760) 넘게 떨어진다 — 235% 줌(D162)에서는 화면
    밖이라 **검색은 됐는데 아무것도 안 뜬 것과 같았다.** 트리 노드는 아니므로
    간선은 안 생기고(tree.ts), 연결선은 점선으로만 그린다. 종류마다 열이
    하나다 — 한 줄로 쌓으면 도판 3 + 클립 2가 1500px 기둥이 된다.
    카메라도 함께 바뀐다(`lib/canvas2/focusCamera.ts`): 딸린 것이 없으면
    카드 하나가, 있으면 **묶음이 다 들어오는 배율**(하한 1.15). 235%
    에서 폭 560 카드는 화면을 꽉 채워 옆에 뭘 놓아도 안 보인다.
  · **235%는 상한이지 고정값이 아니다**(D166, `focusCamera.ts`). D162가
    배율을 고정한 탓에 폭 560 카드가 화면 위 **1316px**을 먹었다 — 교실
    노트북(1366)·구형 크롬북(1024)에서는 화면보다 넓어 **양쪽이 다 잘렸고**
    줄마다 첫 글자가 왼쪽 레일 밑에 깔렸다(실측 2026-08-04, 1024폭: 왼쪽
    114px·오른쪽 178px이 화면 밖). 학생 눈에는 "답이 안 뜬다"였다. 이제
    상한 안에서 **카드가 다 들어오는 만큼만** 당긴다 — 넓은 화면은 그대로
    235%다. **잘린 큰 글씨보다 온전한 작은 글씨가 읽힌다.**
    · 잣대는 창이 아니라 **쓸 수 있는 자리**다. 캔버스 위에는 UI가 늘 떠
      있어(`UI_LEFT/TOP/RIGHT/BOTTOM`) 창 전체로 재면 딱 맞췄다고 계산한
      카드가 레일 밑에 깔린다. D163은 딸린 것이 있을 때만 뺐는데, 잘리는
      건 양쪽 다 마찬가지였다 — 이제 두 갈래가 같은 여백을 본다.
    · **세로는 재지 않는다.** 글은 아래로 자라므로 높이까지 맞추려 들면
      긴 답일수록 축소되어 D162가 뒤집힌다. 위 1/3에 두는 것이 그 답이다.
    · **넘칠 때는 `pad`를 안 쓴다.** 여백은 들어갈 때 숨 쉴 자리다. 이미
      넘치는 묶음에서 왼쪽을 72px 비우면 그만큼이 그대로 오른쪽 밖으로
      밀린다(실측: 배율이 하한에 걸린 1280 화면에서 클립이 딱 그 폭만큼
      잘렸다). 넘치면 UI 안쪽 변에 붙인다.
  · **검은 상하좌우 이동 막대는 걷어냈다**(D157 폐기, 사용자 지시
    2026-08-04). 방향키 이동(`navigate.ts`)은 그대로 남아 있다 — 없앤
    것은 화면 위의 막대뿐이다.
  · **손으로 써서 묻는다**(D171, `AskInkLayer.tsx` · `lib/canvas2/useInkCanvas.ts`
    · `lib/canvas2/penPad.ts` · `lib/api/ocr.ts` · `routers/ocr.py` ·
    `services/ocr.py`). 도구 레일의 **질문하는 펜**(단축키 `q`)을 고르면
    **학습 캔버스 전체가 종이**가 된다. 프롬프트창 위의 작은 입력판으로
    시작했다가 2026-08-04 사용자 지시로 옮겼다 — 상자에 갇혀 있을 때는 한
    줄짜리 질문밖에 못 썼다.
    · **버튼 자리가 단계를 말한다.** 보내기 → [글자 인식](학생의 틸) →
      인식되면 **둘로 갈라진다**: [다시 쓰기] [AI에게 묻기]. 한 자리에서
      바뀌므로 다음에 무엇을 할지 찾아다닐 필요가 없다.
    · **획은 기존 펜(Excalidraw 자유선)이 받는다**(사용자 지시 2026-08-04:
      "기존 펜의 글씨 써지는 라이브러리만 사용"). 투명 캔버스를 얹어 직접
      그려 봤는데 **획 사이가 끊겨 보였다** — 이미 자연스럽게 써지는 것이 같은
      화면에 있는데 흉내를 고치는 것은 순서가 틀렸다. 형광펜이 자유선을 물려
      쓰는 것과 같은 방식이다(D150). 획 스타일도 안 건드린다: 화면에 무슨
      색으로 그려지든 보내는 그림은 **점에서 다시 그린다.**
      · **질문 획과 그림 획은 획 자신이 구분한다**(사용자 지시 2026-08-05,
        `lib/canvas2/askInk.ts`). Excalidraw에는 "새 도구를 등록하는" API가
        **없다** — 공식 문서가 주는 것은 두 가지다: 내장 도구를 물려 쓰는 것과,
        요소에 앱 데이터를 얹는 `customData`(`Record<string, any>`,
        `initialData`나 `updateScene`으로 설정). 그래서 획에
        `customData.nodiAsk`를 찍는다 —
        Excalidraw가 요소와 함께 저장·복원하므로 도구를 오가든 새로고침을
        하든 **질문 획은 계속 질문 획이다.** 처음엔 **시점**으로 갈랐는데
        (도구를 켠 뒤 생긴 것), 도구를 잠깐 바꿨다 돌아오면 앞서 쓴 질문 획이
        **미아**가 됐다 — 인식도 안 되고 지워지지도 않은 채 남는다.
        · 표시를 **찍는** 범위는 여전히 시점으로 좁힌다(켠 순간의 id 집합).
          안 그러면 앞서 일반 펜으로 그린 그림에까지 표시가 번진다(실측:
          질문 1획 + 그림 1획인데 3획으로 셌다). **찍기는 시점, 판정은 표시.**
        · ⚠️ **그리는 중에는 씬을 되쓰지 않는다.** `updateScene({elements})`는
          `replaceAllElements`이고, 공식 문서가 드래그·리사이즈 중 사용을 금한다.
          획을 세는 신호(`onChange`)에서 표시를 찍었더니 **그리던 획이 그 자리에서
          끊겼다** — 사용자 보고 2026-08-05 "글자인식 펜이 작성되지 않는다".
          그리는 동안에는 **세기만** 하고, 표시는 도구를 바꾸거나 인식할 때
          (획이 끝난 뒤의 discrete한 순간) 한 번에 찍는다. e2e가 한 번에 길게
          그어 "한 획이면 정확히 1획"을 확인한다 — 쪼개지면 여기서 깨진다.
        · 지운 획(`isDeleted`)은 뺀다 — 안 그러면 학생이 지운 획이 OCR로 간다.
          인식 뒤에는 **걸러 내지** `isDeleted`로 남기지 않는다(다시 잡힌다).
        · 표시가 씬에 남는 것은 성질이다 — e2e는 매번 **빈 대화**에서 시작한다.
      · **`isPassThroughTool`에 넣어야 한다.** 빠뜨렸더니 **글 위에서 시작한
        획이 통째로 사라졌다**(실측 2026-08-04: 세 획 중 둘만 남았다) —
        오버레이 아이템이 포인터를 먹는다. `DRAW_TOOLS`에는 안 넣는다:
        거기 넣으면 색 팔레트가 딸려 오는데 질문 필기는 색이 의미 없다.
      · **필기는 저장되지 않는다**(사용자 결정). 글자가 되는 순간 씬에서
        빼낸다 — 입력 수단이지 남길 내용이 아니다.
      · **획 수는 저장 신호로 세지 않는다.** `onSceneCommit`에는 1.5초
        디바운스가 걸려 있어서, 쓰는 동안 타이머가 계속 밀리고 **손을 멈춘
        뒤에야 버튼이 켜졌다**(사용자 보고 2026-08-05: "글씨를 쓰면 왜 버튼이
        활성화되지 않지?"). 디바운스 **앞의** `onSceneChange`로 센다 — 저장과
        화면 반응은 리듬이 다르다. e2e는 마감을 **700ms**로 둔다: 넉넉한
        폴링으로 재면 결국은 켜지므로 이 결함이 안 잡힌다.
    · **곧바로 보내지 않는다.** 손글씨 OCR은 "빛"과 "및"을 바꾼다 — 그대로
      나가면 학생은 자기가 안 한 질문의 답을 받는다. 글자는 입력창에
      **덧붙고**(덮어쓰면 앞 구절이 사라진다) 보내는 것은 학생이 누른다.
    · **인식 실패는 획을 지우지 않는다.** 지우면 다시 써야 한다.
    · 백엔드는 `POST /api/ocr/handwriting` — 자체 GPU의
      **VARCO-VISION-2.0-1.7B-OCR**로 넘긴다(실측 3~8초). 주소는
      `JUDGE_BASE_URL`에서 유도한다(같은 기계의 다른 GPU) — 다른 기계면
      `OCR_BASE_URL`. **모델 서버를 브라우저가 직접 부르지 않는다**: 그쪽은
      인증이 없고 CORS가 열려 있어 주소가 곧 공개 GPU 창구가 된다.
      미설정은 501(→"준비하고 있어요"), 모델 서버가 안 받으면 502(→"잠시
      안 돼요"), 빈 결과는 **200**이다(못 읽은 것은 학생 잘못이 아니다).
    · **보내는 그림은 획들의 bbox만 잘라 담는다**(사용자 지시 2026-08-04·05).
      질문 획 전부의 **min(x,y)·max(x,y) 두 점을 네 점으로 펴** 사각형을
      만들고(`strokesBBox`·`bboxCorners`), `renderInkPng`이 그 상자에
      여백 18px을 더해 자른 뒤 **흰 종이에 검은 획**으로 다시 그린다.
      화면 전체를 보내면 여백이 대부분이라 글자가 작아지고, 격자·학생이 그린
      그림이 노이즈로 들어간다. bbox는 **[글자 인식]을 누른 그 순간** 잡는다.
      좌표는 절대 좌표로 편다 — 안 그러면 모든 글자가 왼쪽 위에 겹친다.
      e2e가 보낸 PNG의 **실제 크기**를 재서 이걸 지킨다.
    · **손날은 첫 획부터 막힌다.** `pointerType==='touch'`인데 펜이 활동
      중이면(`penGuard.penActive`, **hover 포함**) 버린다 — 태블릿에서는 펜을
      쥔 손이 먼저 닿는다. 판이 펜을 본 적 있는지만 보면 그 첫 접촉이 낙서가
      된다. ⚠️ **정전식 펜은 `"touch"`로 온다** — 웹에서 손가락과 구분할 수
      없다. 능동식(Apple Pencil·S펜·USI)만 `"pen"`이다. 기기가 무엇을
      보고하는지는 `?pendebug=1`로 화면에서 읽는다.
    · **빗장은 `isPrimary`로 건다.** `pointerRef !== null`로 잠갔더니 앞 획의
      pointerup을 놓칠 때(포인터 캡처를 잃으면 실제로 일어난다) **빗장이 굳어
      다음 획이 통째로 무시**됐다(사용자 보고: "한번씩 입력이 씹힌다").
      열린 채 남은 앞 획은 버리지 않고 닫는다.
    · **획을 긋는 동안 바깥은 그 제스처를 못 본다**(버블 차단). hover는 흘린다
      — 손날 판정이 창 전체에서 펜을 지켜보는 신호가 그것이다.
    · **한글은 획 단위로 끊어 쓴다** — "한" 한 글자가 획 여섯이고 그 사이마다
      펜이 떴다 붙는다. `ask-ink.spec.ts`가 그 순서를 그대로 태워
      (hover → 닿음 → 이동 → 뗌 → hover) 그은 만큼 남았는지를 잰다. 획 수는
      픽셀로 못 센다(획끼리 겹친다) — 그래서 레이어가 `data-strokes`를 낸다.
    · **`inkPhase`는 파생값이다.** state로 들고 이펙트에서 맞추면 도구와 단계가
      어긋나는 순간이 생기고(렌더 한 번 사이) React Compiler도 막는다. 같은
      이유로 인식 결과는 프롭이 아니라 **명령형 손잡이**로 입력창에 넣는다.
  · **연결선 앵커는 상대 위치가 정한다**(D126, `lib/canvas2/connector.ts`).
    고정 앵커였을 때 같은 부모의 답 둘이 한 점에서 나가 선이 겹쳤다. 끝점은
    글자 사각형이 아니라 **패딩 상자**(hover 박스와 같은 크기)의 변에 앉는다.
    기하는 컴포넌트가 아니라 lib에 둔다 — 이런 결함은 눈보다 테스트로 잡는다.
  · **드래그 중 연결선은 `dragBus`로 따라간다**. 아이템이 React 밖에서 DOM
    transform으로 움직이므로 연결선도 React 밖에서 따라가야 한다 — state로
    되돌리면 드래그 성능이 무너진다.
  · 카메라는 CSS transition이 아니라 **임계 감쇠 스프링**(D124). v1은 매 프레임
    setCamera + transition 0.7s로 "따라오다 마지막에 훅 가는" 지연이 있었다.
- **EBS 강의 클립 추천 (D149)**: admin이 EBS 영상 링크를 패키지(학년·과목)로
  묶고, 선생님이 워크스페이스에 켠다(`class_lecture_packages`). 인제스트는
  `worker/lectures.py` — `lecture_parse`(EBS 챕터 파싱 + 자막/Whisper 전사)
  → `lecture_embed`(제목+본문 → Qdrant `lecture_clips`) → `lecture_atom`
  (PIKE 원자 질문 → `lecture_clip_atoms`). 검색은 `services/lecture_search.py`
  가 figure_search를 미러하되 스코프가 file_id가 아니라 **package_id**다
  (거리 게이트: 직접 0.60 / 원자 0.45). 캔버스에는 `kind='clip'` 아이템으로
  뜨고 `page_url`은 안정적이라 **영속한다**(도판과 달리 재발급 없음).
- 교과서 도판은 `services/figure_search.py`가 검색한다(D111 — 프론트 선행
  `/retrieve` 제거, ReAct 스킬과 레거시 경로가 같은 구현을 쓴다). (D94, 사용자
  결정 2026-07-18: EBS 영상·SVG 아트 추천 기능 전면 제거 — `/art/search`·인제스트
  스크립트·Qdrant ebs/art_assets 컬렉션·art_assets 테이블 포함. 마이그레이션
  0040은 2026-07-19 원격 적용 완료).

- **학습 효과는 캔버스가 아니라 붙인 활동이 만든다 (D138)**. 조사 결론이다 —
  공간 배치 자체의 학습 이득 근거는 약하고, 강한 것은 **인출·구성 행위**다.
  · Bastani et al. 2025(PNAS): 답을 그냥 주는 AI로 공부한 고교생은 AI를 치운
    시험에서 **AI를 안 쓴 학생보다 17% 낮았다.** 개념 카드가 그 형태다.
  · Karpicke & Blunt 2011(Science): 인출 > 개념도 작성. **학생 체감은 정반대**다
    — 만족도·지각된 학습으로 이 제품을 판정하면 안 된다.
  · Chi & Wylie 2014(ICAP): 드래그·태그 변경은 Active일 뿐 등급을 못 올린다.
  그래서 "안 보고 다시 말해보기"(D138)를 붙였다가 **2026-08-02 사용자
  지시로 걷어냈다**(D149). 조사 결론 자체는 그대로다 — 지금 이 제품에는
  인출을 강제하는 장치가 **하나도 없고**, 남은 상호작용(드래그·태그·
  다시 질문하기)은 전부 Active 등급이다. 다시 넣는다면 다음 후보는
  **선응답 게이트** — 답 주기 전에 학생이 먼저 한 줄 쓰게 하기
  (Bastani의 가드레일 · Kestin et al. 2025의 공통 메커니즘).
- **분류 태그 목록은 항상 주입한다 (D135)**. 세션의 기존 태그를 프롬프트에
  넣지 않으면 모델이 매 턴 새 태그를 지어 열이 파편화된다(실측: 개념 18개가
  태그 10종, 열당 1.5개). 출처는 **canvas_items.tag**다 — nodes.answer 파싱은
  학생이 직접 고친 분류를 못 본다. 선택 스킬(`list_session_concepts`)에
  맡기면 모델이 안 불러서 보장이 깨진다.

## 불변식 (반드시 유지)

- **RAG는 채팅을 절대 막지 않는다** — 모든 컨텍스트 빌더는 best-effort, 실패 시 None.
- **Qdrant는 신뢰 경계가 아니다** — 사용자 파일 청크 본문은 Qdrant 페이로드에 넣지
  않고, 검색 히트 후 USER 스코프 클라이언트로 재조회해 RLS가 재검증한다.
  (과거 예외였던 EBS·아트 전역 카탈로그는 D94로 제거. canvas_cards는 소유자·
  세션 페이로드 필터를 강제한 채 제목·좌표를 페이로드에 저장한다.)
- **임베딩은 비대칭** — 질의 `embedding-query`, 문서 `embedding-passage`. 혼용 금지.
- **거리 규약** `distance = 1 - score` (Qdrant cosine → 기존 임계값 의미 유지).
- **튜너블(D62)**: admin 오버레이(`app_settings`) > config 기본값. 새 노브는
  `app_settings.as_*` + clamp로 읽고 `db/03_app_settings.sql`에 기본값을 추가해야
  admin 콘솔에 뜬다.
- **권한은 DB가 강제한다(D104)** — RLS 정책 38개 + 함수 26개. 앱 코드로 옮기지
  않는다. 사용자 요청은 `nodi_app` 역할 + `SET LOCAL app.user_id`로 돌고,
  `auth.uid()`가 그 값을 읽어 정책이 판정한다. **직접 커넥션을 얻지 말 것** —
  `db/pool.py`의 `user_conn()`이 트랜잭션과 컨텍스트 주입을 한 묶음으로 보장한다
  (그 경로를 우회하면 앞 요청의 사용자로 질의가 나갈 수 있다).
  워커는 `nodi_worker`(BYPASSRLS). 비밀번호 해시가 든 `public.users`는
  `nodi_app`에 GRANT 자체가 없다 — 정책보다 앞선 방어.
- **아이템 좌표는 저장한다(D122 — D105 폐기)** — 학생이 드래그로 옮길 수 있게
  되면서 D105("좌표 미저장")가 성립하지 않는다. 옮긴 자리를 안 남기면 새로고침
  마다 학생의 작업이 사라진다. 결정론은 `pinned=false` 아이템에서 유지된다.
- **수화는 세션당 한 번이다(D147)** — 세션을 채운 뒤에는 **화면의 글이
  정본**이고, 늦게 도착한 쿼리가 덮을 수 없다. 캔버스 스냅샷(`GET /canvas`)은
  캐시가 아니라 수화용 사진 한 장이라 세션을 떠날 때 버린다. 어기면 이미
  저장된 글이 `_legacy` 복사본으로 바뀌고, 그걸 옮기는 순간 승격 경로가
  **서버에 행을 복제한다**(실측: 카드 3개가 9개). 판정은 `hydration.ts`.
- **세션은 공간에 속한다(D148)** — `activeSessionId`만 보고 열면 학급 공간에서
  개인 세션이 열린다(그 캔버스를 편집하고 질문까지 보낼 수 있다). 스토어의
  `activeSessionSpaceId`가 다르면 **없는 것으로 친다.**
- **파싱은 프론트가 소유한다** — `canvas_items.body`에 AI 응답 원문을 그대로
  담는다. 개념 카드 형식을 파싱하는 구현이 이미 셋이고(streamParser.ts /
  solar.extract_used_tags / ai.skills.concepts._parse_cards) 관용도가 서로
  다르다. 네 번째를 만들면 반드시 어긋난다.

## 개발

- 백엔드 의존성: **`cd backend && uv sync --group dev`** (`uv.lock` 기준 버전 고정).
  `requirements.txt`는 하한만 있는 폴백 — 버전이 팀원마다 갈리므로 권장하지 않는다.
- 백엔드 테스트: `cd backend && uv run pytest tests/ -v` (전부 mock — 키·네트워크 불필요).
  커버리지는 `--cov` 추가(목표치 강제 없음 — 어디가 비었는지 보는 용도, D105).
- 프론트 테스트: `cd frontend && npm test` (vitest). 대상은 `lib/canvas2`의
  순수 함수 — **배치 엔진의 무겹침 불변식이 가장 중요하다**(무작위 200케이스).
  컴포넌트 렌더 테스트는 없다.
- **eslint에 React Compiler 규칙이 켜져 있다.** 렌더 중 ref 쓰기와 이펙트 내
  동기 setState가 **에러**다. 억제하지 말고 구조로 풀 것.
- **dev 서버를 띄운 채 `npm run build`를 돌리지 마라** — `.next`를 덮어써서
  dev가 stale CSS를 내보낸다.
- **`globals.css`를 고쳤으면 `rm -rf .next` 후 dev를 다시 띄운다**
  (2026-08-04 실측, Next 16.2.9 + Turbopack). 이 파일만 유독 **저장해도,
  내용을 바꿔도, 서버를 재시작해도** 다시 컴파일되지 않는다 — 브라우저에는
  옛 규칙이 그대로 오고 청크 해시조차 안 바뀐다. **화면에는 "안 고쳐진 것"으로
  보여서** 코드를 의심하며 시간을 버리게 된다. 확인법은 서버가 실제로 주는
  CSS를 읽는 것이다:
  `curl -sL localhost:3000/login | grep -o '/_next/static/[^"]*\.css'` → 그
  주소를 `curl`해서 방금 넣은 선언을 찾는다. 없으면 캐시다.
  · 같은 캐시가 더 나쁘게 깨진 적도 있다 — 재시작 후 **모든 라우트가 404**
    ("Ready in 289ms"로 뜨는데 페이지가 없다). 처방은 같다.
- **다른 기기에서 dev 서버에 접속할 때 두 가지가 막는다** (2026-08-04 실측,
  `http://10.101.66.6:3000`에서 로그인 불가로 드러났다). 둘 다 CORS처럼 보이지만
  **CORS 오류는 나지도 않았다** — 브라우저가 CORS를 말할 기회조차 없었다.
  · **Next 16은 `/_next/*`를 localhost에서만 준다**(HMR 웹소켓·dev 자산).
    LAN 주소로 들어오면 그 요청만 막히는데, HTML은 그려지므로 화면은 멀쩡하고
    **하이드레이션만 안 된다** — 입력이 React state에 닿지 않아 로그인 버튼이
    영영 비활성이다. `next.config.ts`의 `allowedDevOrigins`가 이 기계의 LAN
    IPv4를 자동으로 넣는다(손으로 적으면 DHCP로 주소가 바뀔 때 같은 증상이
    다시 난다). 다른 주소는 `DEV_ORIGINS=`로 더한다.
  · **`NEXT_PUBLIC_API_BASE_URL`은 브라우저가 부르는 주소다.**
    `http://localhost:8000/api`로 두면 다른 기기의 브라우저에서는 **그 기기
    자신**을 가리켜 연결부터 실패하고(백엔드는 127.0.0.1:8000에만 바인딩한다),
    이 맥에서 IP로 들어오면 Origin이 `cors_origins`(localhost:3000)에 없어
    preflight가 400이다. **`/api`로 두면 둘 다 없다** — 페이지와 같은 출처라
    CORS 자체가 없고, Next가 서버 사이드로 백엔드에 넘긴다(배포와 같은 구성).
- 로컬 실행 (README '빠른 시작' 참조):
  1. `docker compose up -d` (postgres 5433 + qdrant 6333)
  2. `cd backend && uv run uvicorn app.main:app --reload --port 8000`
  3. `cd frontend && npm run dev` (http://localhost:3000)
- 프론트 타입/빌드 스모크: `cd frontend && npx tsc --noEmit && npm run build`
- **API 경로(D105)**: 백엔드 도메인 라우터는 전부 `/api` 아래다. `/health`만
  접두사 밖(인프라 liveness 계약). 프론트 `NEXT_PUBLIC_API_BASE_URL`은 로컬
  `http://localhost:8000/api`, 배포 `/api` — 뒤 경로가 양쪽에서 같다.
- **환경 변수: `backend/.env`** (루트 `.env` 아님 — `config.py`의 `BACKEND_ENV`).
  프론트는 `frontend/.env.local`. 각각 `.env.example`·`.env.local.example` 참고.
  백엔드 필수: `DATABASE_URL`·`DATABASE_WORKER_URL`·`JWT_SECRET` +
  `UPSTAGE_API_KEY`·`EXAONE_API_KEY`. 프론트는 `NEXT_PUBLIC_API_BASE_URL` 하나.
- **설정 자가진단**: `GET /health/config` — 무엇이 빠졌는지 `blocking`·`judge.missing`이
  알려준다(D97, 비밀값 미노출). 같은 요약이 부팅 시 터미널에도 찍힌다.
- **DB 스키마(D104)**: `db/`의 SQL 5개가 이름순 자동 적용된다(빈 볼륨일 때 1회).
  바꾸려면 `01_schema.sql`을 고치고 `docker compose down -v && up -d`.
  **`down -v`는 데이터를 지운다** — 계정도 사라지므로 다시 만들어야 한다.
- **계정은 시드하지 않는다**. 학생·교사는 `/signup`에서 가입하고, 관리자는
  가입으로 얻을 수 없어(D99) CLI로 만든다:
  `uv run python -m app.cli create-user <이메일> <비번> --role admin`
  (`grant-admin` / `list-users`도 있다).

## 컨벤션

- 주석·docstring·커밋 메시지는 **한국어**, 제약·근거 위주. 커밋 프리픽스
  `[feat]:`/`[fix]:`/`[docs]:`/`[tune]:`/`[chore]:`.
- 설계 결정은 D-번호(D11, D35, D62 …)로 코드 주석에 남긴다.
- 스펙: `docs/superpowers/specs/`, 구현 계획: `docs/superpowers/plans/`.
- `.env`·`qdrant_storage/`는 커밋 금지(.gitignore).
