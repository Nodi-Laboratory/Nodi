"use client";

/**
 * 도움말 (사용자 지시 2026-08-09).
 *
 * 지금은 **설명글만** 둔다 — 사용자가 "나중에 변경될 가능성이 높다"고 했다.
 * 그러니 여기에 화면 장치를 얹지 않는다. 글을 갈아 끼우는 것이 다음에 할
 * 일이고, 지금 구조를 세워 두면 그 교체가 더 비싸진다.
 */

const 절 = [
  {
    제목: "무엇을 하는 곳인가요",
    글: [
      "궁금한 것을 물어보면 답이 **개념 카드**로 캔버스 위에 놓입니다.",
      "카드는 읽기만 하는 것이 아니라 옮기고, 고치고, 이어 붙일 수 있어요.",
    ],
  },
  {
    제목: "묻는 방법",
    글: [
      "아래 입력창에 적고 Enter를 누르면 됩니다.",
      "도구 막대의 **질문하는 펜**을 고르면 캔버스에 손으로 써서 물을 수도 있어요. 카드를 동그라미 치거나 화살표로 가리키면 그것도 함께 읽습니다.",
    ],
  },
  {
    제목: "카드 다루기",
    글: [
      "카드를 끌어 옮기면 그 자리가 저장됩니다.",
      "카드 위·아래의 점에서 끌어 다른 카드에 놓으면 두 카드가 이어집니다.",
      "이어진 카드는 부모보다 위로 올라가지 않아요 — 위에서 아래로 읽는 순서를 지키기 위해서입니다.",
    ],
  },
  {
    제목: "찾아다니기",
    글: [
      "오른쪽 위 **지도**를 열면 대화 전체가 한눈에 보이고, 분류나 카드를 누르면 그 자리로 갑니다.",
      "왼쪽 **기록**에서 지난 대화방을 열거나 새 대화를 시작할 수 있어요.",
      "왼쪽 **세션**에서 개인 세션과 학급을 고릅니다.",
    ],
  },
];

export default function HelpPage() {
  return (
    <main className="mx-auto flex w-full max-w-2xl flex-col gap-8 px-6 py-10">
      <h1 className="text-2xl font-semibold text-fg">
        도움말
      </h1>
      {절.map((s) => (
        <section key={s.제목} className="flex flex-col gap-2">
          <h2 className="text-[15px] font-semibold text-fg">
            {s.제목}
          </h2>
          {s.글.map((line) => (
            <p key={line} className="text-sm leading-relaxed text-fg-muted">
              {line.replace(/\*\*/g, "")}
            </p>
          ))}
        </section>
      ))}
    </main>
  );
}
