# nodi backend — 환경 변수 예시
#
# 사용법: 이 파일을 같은 폴더의 `backend/.env`로 복사한 뒤 값을 채운다.
#
#   cp backend/.env.example backend/.env      # PowerShell: copy backend\.env.example backend\.env
#
# ⚠️ 위치 주의: 백엔드는 **`backend/.env`**를 읽는다 (저장소 루트 `.env` 아님).
#    app/config.py 의 BACKEND_ENV 참조. 루트에 두면 값이 하나도 안 읽히는데
#    서버는 정상 부팅하므로(키 없이 부팅하도록 설계) 원인을 찾기 어렵다.
#
# 채운 뒤 확인: 서버를 띄우고 http://localhost:8000/health/config 를 연다.
#    `"ready": true` 면 채팅 한 턴에 필요한 설정이 다 갖춰진 것이다.
#    `blocking` 배열에 남은 항목이 있으면 그게 아직 빠진 것이다.
#
# `backend/.env`는 gitignored다. 실제 키를 이 예시 파일에 적지 말 것.

# ============================================================================
# 필수 — 이 4개가 없으면 앱이 기능하지 않는다
# ============================================================================

# --- Supabase (인증 · 데이터 · Storage) ---
# Settings > API 에서 복사.
SUPABASE_URL=                    # 예: https://<ref>.supabase.co
SUPABASE_ANON_KEY=               # publishable 키 (sb_publishable_... — 신규 포맷)
# 미설정 시 파일 업로드·임베딩 워커가 전부 503. 비밀이므로 절대 노출 금지.
SUPABASE_SERVICE_ROLE_KEY=

# --- Upstage (임베딩 4096d + 문서 파싱) ---
# 미설정 시 RAG·파일 인제스트·교과서 처리가 통째로 죽는다.
UPSTAGE_API_KEY=                 # up_...

# --- EXAONE / Friendli (채팅 응답 생성) ---
# 미설정 시 채팅 스트리밍이 503.
EXAONE_API_KEY=                  # Friendli API 키 (flp_... 로 시작)

# ============================================================================
# 선택 — 기본값이 있거나, 특정 기능에만 필요
# ============================================================================

# --- Qdrant (벡터 저장소) ---
# 로컬은 `docker compose up -d qdrant` 후 기본값 그대로 두면 된다.
QDRANT_URL=http://localhost:6333

# --- EXAONE 전용(dedicated) 엔드포인트 ---
# 설정하면 /dedicated/v1 로 요청(예약 GPU → 공유 rate limit 없음).
# 비우면 serverless로 요청. Friendli 대시보드에서 엔드포인트 생성 후 ID를 넣는다.
# EXAONE_ENDPOINT_ID=
# EXAONE_MODEL=LGAI-EXAONE/K-EXAONE-236B-A23B
# FRIENDLI_BASE_URL=https://api.friendli.ai

# --- 교과서 figure 캡션 판정 (VLM) — 교과서 업로드 기능에만 필요 ---
#
# ⚠️ D93 이후 **판정은 필수 게이트다.** 아래 3개 중 하나라도 비어 있으면
#    교과서(kind='textbook') 업로드가 503으로 거부된다. 503 메시지가 빠진
#    키 이름을 알려준다. (수업자료 class_material·학생 업로드는 무관하게 동작.)
#
# ⚠️ D97: JUDGE_BASE_URL에는 기본값이 없다 — 반드시 명시한다.
#    (과거 기본값은 TTA 게이트웨이 30099를 가리켰으나, 그 포트는 프론트엔드로
#     용도가 바뀌었다. 키만 채우면 비전 요청이 엉뚱한 서비스로 날아갔다.)
#
# 교과서 기능을 쓰지 않는 환경이면 **세 값을 모두 비워 둔다** — 업로드가 명확히
# 거부되므로, 올라간 뒤 도판만 조용히 사라지는 사고가 없다.
#
# OpenAI 호환 비전 엔드포인트면 무엇이든 꽂을 수 있다. 단 요청 파라미터가
# EXAONE 4.5 실측값(chat_template_kwargs.enable_thinking 등)이라 다른 모델에서는
# 무시되거나 400이 날 수 있어 검증이 필요하다 — services/figure_judge.py 참조.
JUDGE_API_KEY=
JUDGE_BASE_URL=                  # 예: http://localhost:8081/v1
JUDGE_MODEL=EXAONE-4.5-33B

# --- 앱 (기본값이 합리적 — 보통 건드리지 않는다) ---
# ENVIRONMENT=development
# JWT_AUDIENCE=authenticated
# SUPABASE_PROJECT_REF=           # 비우면 SUPABASE_URL에서 파생
# SUPABASE_JWKS_URL=              # 비우면 ${SUPABASE_URL}/auth/v1/.well-known/jwks.json
