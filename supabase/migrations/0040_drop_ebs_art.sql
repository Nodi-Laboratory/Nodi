-- ============================================================================
-- nodi — migration 0040 (EBS·아트 기능 제거; D94, 사용자 결정 2026-07-18)
--
-- 캔버스의 EBS 영상·SVG 아트 추천 노드 기능이 제거됨에 따라(백엔드 retrieve
-- 레그·/art/search 라우터·인제스트 스크립트 삭제) 잔존 DB 객체를 정리한다.
--
-- 이력: art_assets는 0027이 생성(pgvector 검색), 0028이 RPC(search_art_assets)
-- 드랍, 0033이 embedding 컬럼 드랍 — 남은 것은 메타 테이블 본체뿐이고 코드
-- 독자·기록자 0이다(2026-07-18 grep 실측). ebs는 Supabase 테이블이 없었다
-- (카탈로그 JSON → Qdrant 직행). Qdrant의 ebs/art_assets 컬렉션은 코드가 더는
-- 생성·참조하지 않으며, 기존 인스턴스의 잔존 컬렉션 정리는 수동
-- (DELETE /collections/{name}) — TASK 1의 textbook 컬렉션 정리와 동일 관례.
--
-- 멱등. Apply via Supabase MCP apply_migration (사용자 승인 후). AFTER 0001..0039.
-- ============================================================================

drop table if exists public.art_assets;

-- ============================================================================
-- End of 0040_drop_ebs_art.sql
-- ============================================================================
