-- 한 번에 추천할 사진 수를 1로 (사용자 지시 2026-08-12)
--
-- 곁들이는 답을 **거드는 것**이지 답과 나란히 설 것이 아니다. 3장이 붙으면
-- 카드 옆에 그림 기둥이 서고, 235% 줌(D162)에서는 그중 뒤쪽이 화면 밖이다.
--
-- ⚠️ 튜너블은 `app_settings` 행이 config 기본값을 이긴다(D62). 코드만 고치면
-- 이미 돌고 있는 DB에서는 **아무 일도 안 일어난다** — 그래서 행도 옮긴다.
--
-- ⚠️ **관리자가 일부러 바꾼 값은 안 건드린다.** 옛 기본값(3)인 행만 옮긴다.
-- 멱등이다 — 여러 번 돌려도 같다.

UPDATE public.app_settings
   SET value = '1'
 WHERE key = 'figure_retrieve_top_k'
   AND value = '3';
