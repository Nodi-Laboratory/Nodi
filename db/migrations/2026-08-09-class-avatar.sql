-- 학급 프로필 사진 (사용자 지시 2026-08-09).
--
-- 세션 선택 화면은 학급을 **그림으로** 알아보게 한다 — 이름 첫 글자 하나로는
-- "3학년 1반"과 "3학년 2반"이 구분되지 않는다. 사진은 선생님이 정한다.
--
-- 파일 자체는 Storage에 두고 여기에는 **경로만** 남긴다(도판·썸네일과 같은
-- 방식). 서명 URL을 저장하지 않는 이유는 D87에 적혀 있다 — 만료되면 깨진
-- 주소가 DB에 남는다.
--
-- 멱등이다. 이미 있으면 아무 일도 안 한다.
ALTER TABLE public.classes
  ADD COLUMN IF NOT EXISTS avatar_path text;
